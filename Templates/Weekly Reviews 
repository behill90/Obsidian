# Weekly Review — {{date:gggg-[W]WW}}

## 🎯 This Week Focus
-
-
-



## 🔥 Aging P1s
```dataview
TASK
FROM "Daily Notes"
WHERE !completed
  AND contains(text, "#p1")
  AND file.day <= date(today) - dur(3 days)
SORT file.day asc
```


## 🔥 P1s Completed This Week
```dataview
TASK
FROM "Daily Notes"
WHERE completed
  AND contains(text, "#p1")
  AND completion >= date(today) - dur(7 days)
```

## ⏳ Still Waiting
```dataview
TASK
FROM "Daily Notes"
WHERE !completed AND contains(text, "#waiting")
```

## 📊 Work Mix (last 7 days)
```dataviewjs
const pages = dv.pages('"Daily Notes"')
  .where(p => p.file.day >= dv.date("today").minus({ days: 7 }));

let counts = { "🧠":0, "🧯":0, "🧭":0, "🔧":0 };

for (const p of pages) {
  for (const l of (p.file.lists ?? [])) {
    for (const e of Object.keys(counts)) {
      if (l.text?.startsWith(e)) counts[e]++;
    }
  }
}

dv.table(
  ["Type", "Count"],
  Object.entries(counts)
);

```